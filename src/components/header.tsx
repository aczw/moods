import { NavLinks } from "./nav-links";

const Header = () => {
  return (
    <header className="w-full bg-red-100">
      header here wow
      <NavLinks />
    </header>
  );
};

export { Header };
